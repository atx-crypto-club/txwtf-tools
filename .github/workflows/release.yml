name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  test:
    # NOTE: the expensive entries here are macOS and Windows, not Linux --
    # GitHub bills macOS at 10x and Windows at 2x the Linux minute rate, so one
    # release costs 4 macOS jobs at 10x plus 4 Windows jobs at 2x. There is
    # nowhere on the fleet to move them: one Windows box (a roaming laptop,
    # deliberately not a gate) and no macOS at all. If minutes stay tight,
    # thinning this matrix is where the saving is, and that is a call about
    # which platforms txwtf-tools promises to support.
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Poetry
        run: pipx install poetry

      - name: Install dependencies
        run: poetry install --all-extras

      - name: Run unit tests
        run: poetry run pytest tests/ -m "not integration and not live" -v --tb=short

      - name: Run integration tests
        if: runner.os == 'Linux'
        run: poetry run pytest tests/ -m "integration and not live" -v --tb=short

  publish:
    needs: test
    runs-on: [self-hosted, Linux, X64]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build

      - name: Build sdist and wheel
        run: python -m build

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true

      - name: Publish to PyPI
        if: env.PYPI_TOKEN != ''
        env:
          PYPI_TOKEN: ${{ secrets.PYPI_TOKEN }}
        run: |
          pip install twine
          twine upload dist/* -u __token__ -p "$PYPI_TOKEN"
